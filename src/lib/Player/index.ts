import Entity from "../Entity";

export default class Player extends Entity {
  public preload() {
    super.preload();
  }

  public create() {}

  public update(delta: number) {
    super.update(delta);
  }
}
